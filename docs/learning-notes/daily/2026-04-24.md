# 2026-04-24 하루 리뷰

> 생성: sq-today-reviewer v0.3 (dry-run · v2 → v3 재작성, ③ 2트랙 분리) / 2026-04-25 02:30 KST
> 대상: 1개 프로젝트 (pasta-japan-server) · 9개 세션 (메인 5 + subagent 4) · 약 8h 간헐 활동
> 본 리뷰의 학습 권장은 **8년차 BE / 시니어→프린시플 전환 단계** 기준

## ① 오늘의 작업 요약

- **pasta-japan-server**
  - **aiInsightReportJob 배치 디버깅 → PR #548** — 2커밋 fix+refac, aiinsight 전체 테스트 통과, base=main — 세션 52a1257d (KST 00:03~11:14)
  - **admin 사용자 상세 "서비스 이용권한 탭"** 구현 완료 — 쿠폰/구독/그룹/병원 필드 정의 + 문서화 — 세션 dfa82305 (KST 12:26~19:25)
  - **혈당 복구 기능 고도화 설계 검토** — 파스타ID → 기기목록 → 센서 그래프 — 세션 dfa82305
  - **Spring Boot 부팅 실패 즉시 복구** — `application.yml`의 `admin:` 키 중복 → SnakeYAML 2.2 `DuplicateKeyException` — 세션 1d9b0f1a (KST 12:46~12:50, 4분)
  - **PR 자체 코드 리뷰 4종 병렬** — 효율성/품질/재사용성/종합. 필수 지적 3건(타임존 하드코딩, 타임존 유틸 중복, 센서 데이터 전량 로드) — subagent agent-a2065/a7f1/a267/aa26 (KST 13:46)
  - **api 공통 필터 402 대응** — `ServiceAccessAuthorizationFilter` 비활성화 후 Cloud Run 배포 + 1분 관측 — 세션 bc81ad5f (KST 17:20~19:15)

**오늘의 하이라이트**: PR 리뷰를 **subagent 4종 병렬**로 돌린 도구 사용 판단은 잘했음. 혼자 19파일 훑었다면 놓쳤을 타임존 유틸 중복을 자동으로 잡아냄.

---

## ② 놓친 학습 포인트

### 1) Spring Batch chunk 트랜잭션 경계 — aiInsightReportJob 수정 (세션 52a1257d)

- **[관찰]** PR #548에서 배치 로직 fix+refac 2커밋. 대화 로그상 "chunk 단위에서 실패하면 어떤 row부터 재시작되는가 / `@Transactional` 경계가 chunk-commit 경계와 일치하는가" 확인 흔적 없음. 테스트 통과 → PR 직행.
- **[원인]** **Claude 신뢰 과잉** 축. 배치는 "테스트 통과 = 프로덕션 안전"이 가장 위험하게 깨지는 영역(부분 실패·재시작·중복 처리), 이 전제를 의심하는 단계가 없었음.
- **[대가]** 프로덕션 중간 실패 시 재시작 동작을 본인이 설명 못 함 → 원인 파악 **2~4h 추가** + 최악 시 **데이터 중복/누락 정정 야근**. aiInsight는 대시보드/리포트 입력이라 정정이 숫자로 드러나 팀장·기획·CS 3자 동시 대응 시나리오.

### 2) `ServiceAccessAuthorizationFilter` 비활성화의 도메인 이해 — 세션 bc81ad5f

- **[관찰]** 402 이슈 해결로 필터 비활성화 결정. 이 필터가 원래 **어떤 케이스에서 402를 뱉었는지 / 비활성화 후 그 트래픽이 어디로 가는지** 사전 정의 흔적 없음. 안전망은 "배포 후 1분 관측 체크리스트" 위임.
- **[원인]** **지식 공백** + **순서 역전** + 시간 압박(KST 17~19시).
- **[대가]** 만약 이 필터가 과금 미납·무료체험 만료 게이트였다면 비활성화 = **유료 콘텐츠 무제한 노출** = 매출 영향 + 보안 감사 이슈. 다음 월 결산 전까지 아무도 모르고 지나가는 것이 가장 나쁜 시나리오.

### 3) Subagent 리뷰 4종 결과의 **통합 판단** 부재

- **[관찰]** 4개 리뷰 중 2개가 **같은 타임존 이슈**를 다른 각도에서 지적(하드코딩 / 유틸 중복). "이 둘은 한 이슈의 두 얼굴"이라는 통합 판단 단계가 대화에 없음.
- **[원인]** **도구 의존 심화**. subagent 돌리는 단계까지는 능숙하나 **결과를 한 장으로 압축하는 메타 단계** 미습관화.
- **[대가]** 독립 처리 시 **중복 PR**, 한쪽만 처리 시 **반쪽 해결**. 팀에서 "subagent 많이 쓰는데 결과 정리가 약하네" 평가 누적 시 **리뷰 부탁이 외면**되는 패턴 가능.

---

## ③ 공부하면 좋을 주제 — 2트랙

### ③-A 실무 익힘 (라이브러리·도구·프레임워크)

다음 PR/배포에서 즉시 적용 가능.

| 주제 | 근거 세션 | 우선순위 | **안 익히면 치를 대가** | 학습 방향 |
|------|----------|---------|---------------------|----------|
| Spring Batch chunk·트랜잭션 경계·재시작 전략 | 52a1257d (PR #548) | **H** | 프로덕션 재시작 버그 시 원인 파악 2~4h + 데이터 정정 야근 1건 가능 | Spring Batch Reference `Chunk-Oriented Processing` → `Transaction Attributes`. 1시간 안 걸림 |
| Java `ZoneId`/`ZonedDateTime` + JS `Intl.DateTimeFormat` 표준 패턴 + 팀 공통 유틸 설계 | subagent 리뷰 3건 | **H** | 관리자 화면 시간 오류 → CS 문의 무한 반복. 지금 팀에 타임존 유틸 없어 매번 재발명 중 | Oracle Java Tutorial *Date Time* 전체 + 팀 공통 유틸 PR 1개 직접 올려보기 |
| Spring Boot Externalized Configuration + SnakeYAML 2.x 엄격도 | 1d9b0f1a | M | 같은 트랩이 `spring:`/`logging:` 등에 남아 있을 가능성. 다음 의존성 업그레이드 시 부팅 실패 반복 | Spring Boot docs *Externalized Configuration* 20분 + SnakeYAML 2.0 릴리스 노트 |
| Cloud Run 배포 후 관측 체크리스트 표준화 | bc81ad5f | M | 매번 Claude에게 받음 → 팀에 지식 안 남음 | 사내 런북부터. 외부는 GCP Cloud Run 공식 *Monitoring* 챕터 |

### ③-B CS·이론 학습 (S/W·CS 깊이) — 8년차 BE 단계 적합

라이브러리 아래 깔린 모델·역사·트레이드오프. **이 트랙은 즉시 효과보다는 향후 1~3년 의사결정권의 동력**.

| 키워드 (영어 병기) | **오늘 작업과의 다리** | 우선순위 | **안 배우면 치를 대가 (시니어 관점)** | 추천 자료 (1차 정통) |
|---|---|---|---|---|
| **멱등성 (Idempotency)** / **At-least-once vs Exactly-once delivery** / **Crash-Recovery model** | Spring Batch 재시작 안전성의 이론적 기초. 오늘 PR #548은 본질적으로 "재시작 시 어떻게 행동해야 하는가"를 묻는 문제였는데, 이 모델 어휘를 본인이 안 가지고 있어 Claude 제안을 평가할 잣대가 없었음 | **H** | 회사가 Kafka·Pub/Sub·Saga 패턴 도입 시 본인이 처음부터 추론 불가 → 시니어로서 신기술 의사결정 자리에서 **기술 영향력 약화**. 8년차+가 분산 시스템 어휘 없으면 팀 내 위치 정체 | **DDIA** (Kleppmann) **Ch.7 Transactions + Ch.11 Stream Processing** + Pat Helland *"Life beyond Distributed Transactions"* (논문, 무료 PDF). DDIA 책 1권은 향후 5년 가성비 최고 |
| **Civil time vs Absolute time** / **Wall-clock vs Monotonic clock** / **IANA tz database 설계 의도** | 오늘 타임존 이슈 3건의 본질. 라이브러리(`ZonedDateTime`)는 결국 이 모델을 구현한 것뿐. 모델을 알면 자바·자바스크립트·DB·다른 언어 어디서도 동일 사고 적용 가능 | **H** | 글로벌 서비스(예: pasta 글로벌 확장)에서 시간·달력·서머타임·윤초 이슈가 **본인이 모르는 곳에서 폭발**. 다국가 서비스 BE는 시간 모델 모르면 시니어 자격 미달 | DDIA **Ch.8** (특히 *Unreliable Clocks* 절) + 클래식 글 *"Falsehoods programmers believe about time"* (gist 검색, 30분 읽기) |
| **AuthN vs AuthZ** / **Fail-open vs Fail-closed** / **Principle of Least Privilege** / **Defense in Depth** | 오늘 `ServiceAccessAuthorizationFilter` 비활성화는 본질적으로 **Fail-open으로 전환**한 결정. 이 용어를 본인이 안 가지고 있으면 비슷한 결정을 또 할 때 같은 사각지대에 빠짐 | **H** | 보안 사고 1건 발생 시 사후 RCA에서 "왜 그렇게 결정했는가"에 본인 어휘로 답 못 함. 시니어가 보안 어휘 없으면 **사고의 책임이 본인에게 더 몰리는** 구조 | **Ross Anderson, *Security Engineering* 3rd Ch.1–3** (PDF 무료 공개) + **OWASP ASVS v4 §4 Access Control** (체크리스트 형식, 30분) |
| **Cohesion / Coupling** / **Information Hiding (Parnas 1972)** / **Modular Design** | 오늘 PR 19파일 32커밋 스코프 혼재의 이론적 분석. "왜 작은 PR이 좋은가"를 1972년 Parnas가 이미 정리. 본인이 이 어휘를 가지면 PR 분할 결정이 직관이 아닌 **원리 기반**이 됨 | **H** | 시니어인데 PR 설계 원리를 본인 말로 설명 못 하면 후배 멘토링 시 "그냥 작게 해" 수준에 그침. 후배가 **왜 그래야 하는가**를 묻는 순간 답할 자료가 없음 | **John Ousterhout *A Philosophy of Software Design* Ch.4–6** (얇음, 한 주에 끝남). 원전 욕심나면 **Parnas 1972** 논문 (5페이지, ACM 무료) |
| **Observability 3 pillars (Logs/Metrics/Traces)** / **SLI · SLO · Error Budget** / **MTTD · MTTR** | 오늘 Cloud Run 1분 관측의 학술적 백본. 지금은 "Claude가 준 체크리스트"지만 SRE 어휘를 가지면 **본인이 회사의 SLO를 정의하고 측정 책임자가 될 수 있음** — 시니어→프린시플 핵심 전환 영역 | M | 8년차+에서 SRE 어휘 없으면 인프라·DevOps팀과의 협상에서 항상 수동적. "관측이 부족합니다"라는 말을 듣고도 무엇이 부족한지 정의 못 함 | **Google SRE Workbook** *Ch.2 Implementing SLOs* + *Ch.4 Monitoring* (무료 온라인). 가벼움 |

#### ③-A ↔ ③-B 다리 정리

- "Spring Batch 잘 쓰기(③-A)"는 **Idempotency·Crash-Recovery 모델(③-B)** 을 모르면 그저 흉내내기. 오늘 PR #548을 **이론 어휘로 다시 설명할 수 있을 때** 본인이 진짜 이해한 것.
- "타임존 라이브러리 잘 쓰기(③-A)"는 **Civil/Absolute time 구분(③-B)** 을 모르면 매번 새 케이스에 무너짐.
- "필터·인증 변경 결정(실무)"은 **Fail-open/closed 어휘(③-B)** 가 있어야 사후 회의에서 본인 결정을 변호 가능.

---

## ④ 반복 실수 패턴

**첫 리뷰 — 관찰 시작.** 누적 회수 나올 때까지(보통 3~5회) 초기 신호로만 기록. 단, 누적 시 대가는 미리 못 박음.

### [초기 신호 A] Claude 제안을 **원리 확인 없이 수락**

- **[오늘 발생]** SnakeYAML(1d9b0f1a), Spring Batch(52a1257d), 필터 비활성(bc81ad5f) 3건 공통.
- **[누적 추이]** 1일차.
- **[누적 대가 경고]** 2주 지속 시 "Claude 없이 기술 판단 못 하는 사람"으로 정체성 굳어짐. 3개월 지속 시 팀 내 "이 사람 리뷰는 Claude 재가공"이라는 인식 → 기술 영향력·승진 평가 반영. AI 협업 시대에도 "본인 판단 근거를 말할 수 있는가"가 변별점.

### [초기 신호 B] PR 브랜치명과 변경 범위 불일치

- **[오늘 발생]** `user-detail-service-access-tab` 브랜치에 혈당 복구 + 공통 JS까지 19파일 32커밋. subagent 재사용성·종합 두 리뷰가 공통 지적.
- **[누적 추이]** 1일차.
- **[누적 대가 경고]** 1개월 지속 시 리뷰어가 본인 PR을 **rubber-stamp** 하기 시작 (PR 크기와 결함 발견율 반비례는 업계 데이터로 확인됨). 사고 발생 시 "리뷰가 못 봤다"가 아닌 "이 PR은 애초에 리뷰 불가능한 크기였다"로 책임이 본인에게 더 몰림.

---

## ⑤ 갖춰야 할 소양

### 1) 보안·인증 변경의 **사전 영향 평가** 습관

- **[관찰]** bc81ad5f에서 필터 비활성 결정 시 "이 필터가 막던 것 / 끄면 트래픽이 가는 곳" 사전 답이 대화에 없음. 배포 후 1분 관측으로 사후 커버.
- **[반복 비용]** 3~6개월에 인증·인가·과금·보안 변경 누적 10~20건. 그중 1건이 매출/법무와 얽히면 **이전 모든 변경이 감사 대상**. 본인이 근거 기억 못 하면 신뢰 회복 어려움.
- **[행동 단위]** Filter/Interceptor/SecurityConfig 건드리는 PR은 **본문에 "이 변경이 막던 것 / 허용하는 것" 2줄 필수 기재**. 코드 주석이 아닌 PR 본문에. (이론 연결: Defense in Depth 관점에서 "지금 어느 층을 끄는가"를 명시하는 것)

### 2) Claude 제시 체크리스트의 **즉시 내재화 vs 일회성 사용** 구분

- **[관찰]** bc81ad5f에서 Claude 1분 관측 체크리스트를 받음. 일회용으로 끝.
- **[반복 비용]** 다음 배포에서 또 받아야 함. 3~6개월 누적 시 "Claude 없이 배포 못 함" 상태 + **팀에 지식이 남지 않음** (옆자리 주니어는 본인이 내재화한 줄 오해).
- **[행동 단위]** Claude 체크리스트 받은 당일 **`docs/runbook/`에 1파일로 저장**. 다음부터는 "기존 체크리스트 개선점"만 물음. (이론 연결: Conway's Law — 본인 도구 사용 패턴이 팀 지식 구조를 만든다)

### 3) PR 열기 전 **`git diff` 30초 의식**

- **[관찰]** 오늘 19파일은 "커밋하면서 자연스럽게 여기까지 온" 결과. subagent가 알려주기 전에 본인이 못 잡음.
- **[반복 비용]** subagent·동료에게 "범위 점검"을 떠넘기는 구조 → 리뷰 rubber-stamp화로 직결.
- **[행동 단위]** `gh pr create` 직전 **`git diff $(git merge-base HEAD main)...HEAD --stat`** 30초 확인. 브랜치 이름과 파일 목록 정합성 검증. shell alias로 박아두면 더 확실: `alias pr-check='git diff $(git merge-base HEAD main)...HEAD --stat'`. (이론 연결: Cohesion — 한 브랜치 = 한 응집된 변경)

---

## ⑥ 내일을 위한 액션 1~3개

1. **PR #548 머지 전, subagent 리뷰 4종을 1장 요약으로 통합** — 중복 지적(타임존 2건 → 1이슈) 병합 + 독립 지적 분리, "머지 전 해결" vs "후속 TODO" 태깅. **15분.**
2. **`docs/runbook/cloud-run-deploy-checklist.md` 신규 생성** — 오늘 bc81ad5f 체크리스트를 본인 말로 다시 써서 저장. 다음 배포부터 이 파일 참조. **10분.**
3. **DDIA Ch.7 (Transactions) 도입부 30페이지만 읽기** — 오늘 ②-1·③-B 첫 항목과 직결. *멱등성·At-least-once·Crash-Recovery* 어휘를 한 번 가지면 다음 배치 PR이 다르게 보임. **30~40분.**

---

*다음 리뷰(2026-04-25)에서 ④ 초기 신호 A·B 재관찰 여부 + ⑥ 액션 이행 여부 우선 확인. 특히 **③-B 1번(DDIA Ch.7)** 이행 시 ② 놓친 학습 섹션의 깊이가 달라질 것 — 본인 어휘로 PR 결정을 설명할 수 있는지가 변별점.*
