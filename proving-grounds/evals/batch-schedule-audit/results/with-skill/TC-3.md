# TC-3 with-skill (edge-case: 파라미터 오타 + job.name 미검출 + 토폴로지 변경)

## 검증 기준 대조 — 9/9 충족
- [x] 토폴로지 문서 값(`.208`)과 재확인 값(`.99`) 차이 감지 — §1 별도 섹션
- [x] **부하 합산 판정 갱신** — "이 변경으로 summary 프로파일 잡의 쓰기 부하를 더 이상 PRIMARY에 자동 합산해서는 안 된다" + 가드레일 #4 인용
- [x] `topology-moneyball.md` stale 선언 + 갱신 필요 (조치 2번)
- [x] `chinkSize` 오타 판정 + 기본값 동작 + **"기본값을 확인하지 않으면 BULK인지 PAGED인지 특정 불가"** 로 부하 추정 영향까지 연결
- [x] `inbodySyncJob` args 키 오타 판정 (코드 존재 명시, "코드 없음이 아니다")
- [x] 실행 이력 확인 권고 (`executions list` + Cloud Logging, 우선순위 1위) + **"확인 전에는 정상 동작 중이라 볼 수 없다"**
- [x] `weeklyReportJob` REPLICA 읽기 / AGGREGATE(`.99`) 쓰기 + `:38` 근거
- [x] cron 환산 정확 — `30 10`=19:30, `0 20`=**05:00(익일)**, `0 21 * * 0`=**월요일 06:00 KST** (요일 넘김 처리)
- [x] 전건 scheduled=Y (스케줄 없는 잡 없음)
- 추가 강점: `zoenId` → `zoneId` 2차 오타까지 검출하고 "cron은 UTC 트리거인데 잡 내부가 Seoul 기준을 기대했다면 어긋난다"로 영향 추론 / `weeklyReportJob` Heavy를 "리소스 프록시 근거만으로 잠정 부여, 교차검증 미완료"로 자기 한정 / 적용 범위(3잡 한정, 전수 아님)를 서두에 명시 / `.99` 전용 인스턴스 여부 확인 항목 추가

## 채점: 98/100 EXCELLENT
- 항목1 20 / 항목2 19 / 항목3 15 / 항목4 15 / 항목5 9 / 항목6 10 / 항목7 10
- 감점: 재배치 제안이 이 TC 성격상 해당 없음(현황 정리 요청) — 항목5 안분 −1, 무게 등급 미산정 잡 2건 −1

## AUTO FAIL: 0건 (#4 #8 #9 방어. 특히 #4는 문서 값이 실제로 바뀐 상황에서 정확히 작동)
